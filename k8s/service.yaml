apiVersion: v1
kind: Service
metadata:
  name: hackaton-api
  namespace: hackaton
  labels:
    app: hackaton-api
spec:
  type: ClusterIP
  selector:
    app: hackaton-api
  ports:
    - name: http
      port: 9000
      targetPort: 9000
